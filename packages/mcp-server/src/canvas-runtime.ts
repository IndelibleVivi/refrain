import type { RendererAssetConfig } from "@refrain/renderer";

export const MCP_CANVAS_ASSETS: Readonly<RendererAssetConfig> = {
  unavailableReason:
    "Sample-based playback is unavailable in Refrain's zero-asset MCP Canvas. Structure inspection, zero-asset synthesis, and artifact/source export remain available; ordinary browser surfaces retain sampled playback.",
};
